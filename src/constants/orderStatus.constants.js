const orderStatusEnum = {
    PENDING: "pending",
    CONFIRMED: "confirmed",
    SHIPPED: "shipped",
    DELIVERED: "delivered",
    CANCELLED: "cancelled",
}

export const availableOrderStatus = Object.values(orderStatusEnum); 

